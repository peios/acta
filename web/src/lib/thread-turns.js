/** @typedef {import('./threads.svelte').ThreadFrame} ThreadFrame */
/** @typedef {{kind: 'turn-ended', id: string, frame: ThreadFrame, label: string, durationMs: number | null, tokens: {label: string, value: number}[], error: string, diff: string | null, diffMode?: string, diffIncomplete?: boolean}} TurnEnding */
export {};
