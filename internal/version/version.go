// Package version identifies the signed release used to build this executable.
package version

// Current is set by the release build; local development has no release version.
var Current = "0.0.0"
